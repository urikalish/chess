export class Player {

	constructor(index, colorType, playerType, army, name) {
		this.index = index;
		this.colorType = colorType;
		this.playerType = playerType;
		this.army = army;
		this.name = name;
	}
}
